import '@testing-library/jest-dom';
import { beforeAll, afterAll } from 'vitest';

// Global test setup
beforeAll(() => {
  // Operations to execute before all tests
});

afterAll(() => {
  // Operations to execute after all tests
}); 